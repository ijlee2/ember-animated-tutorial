{{#if failedToLoadImage}}
    <MaterialIcon @icon="person" />

{{else}}
    <img
        data-test-image
        src={{imageUrl}}
        alt=""
        onerror={{action "loadDefaultImage"}}
    >

{{/if}}