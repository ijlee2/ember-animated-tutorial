<h1
    data-test-name
    {{fade-up}}
>
    {{student.fullName}}
</h1>

<div
    class="section student-details__email"
    {{fade-up id=student.id delay=50}}
>
    <h2>Email</h2>

    <a
        data-test-email
        href="mailto:{{student.email}}"
        title={{student.email}}
    >
        {{student.email}}
    </a>
</div>

<div
    class="section student-details__phone"
    {{fade-up id=student.id delay=150}}
>
    <h2>Phone</h2>

    <a
        data-test-phone
        href="tel:{{student.internationalPhone}}"
        title={{student.phone}}
    >
        {{student.phone}}
    </a>
</div>

<div
    class="section student-details__education"
    {{fade-up id=student.id delay=250}}
>
    <h2>Education</h2>

    <ul>
        {{#each this.resume.degrees as |degree|}}
            <li data-test-degree>{{degree}}</li>
        {{/each}}
    </ul>
</div>

<div
    class="section student-details__experiences"
    {{fade-up id=student.id delay=350}}
>
    <h2>Experiences</h2>

    <ul class="my-list">
        {{#each this.resume.experiences as |experience|}}
            <li
                data-test-experience
                class="item"
            >
                <h3 data-test-title class="item__title">
                    {{experience.position}} at {{experience.company}}
                </h3>

                <ul>
                    {{#each experience.achievements as |achievement|}}
                        <li data-test-achievement class="item__achievement">
                            {{achievement}}
                        </li>
                    {{/each}}
                </ul>
            </li>
        {{/each}}
    </ul>
</div>

<div
    class="section student-details__skills"
    {{fade-up id=student.id delay=450}}
>
    <h2>Skills</h2>

    <ul>
        {{#each (sort-by "type" "name" this.resume.skills) as |skill|}}
            <li
                data-test-skill
                class="item item--{{skill.type}}"
            >
                {{skill.name}}
            </li>
        {{/each}}
    </ul>
</div>